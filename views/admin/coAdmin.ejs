<%- include('partials-admin/header.ejs') %>
    <div class="connexion-admin">


        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    Bienvenue Jeune aventurier, si tu es arrivé jusqu'ici ce n'est pas un hasard! <br> connectez-vous
                    pour afficher le tableau d'administration
                </div>
                <!-- CHARGEMENT DU MESSAGE-->
                <% if(message.length> 0) {%>
                    <div class="alert alert-success mb-2" role="alert">
                        <%= message %>
                    </div>
                    <%}%>
            </div>

            <div class="row">
                <div class="col-md-5">
                    <form action="/admin/connexion?_method=POST" method="POST">
                        <div class="mb-3">
                            <label for="exampleInputEmail1" class="form-label">Adresse Email </label>
                            <input type="email" class="form-control" id="exampleInputEmail1"
                                aria-describedby="emailHelp" name="email">
                                <% if(messemail.length> 0) {%>
                                <div class="alert alert-danger mb-2" role="danger">
                                    <%= messemail %>
                                </div>
                                <%}%>
                        </div>
                        <div class="mb-3">
                            <label for="exampleInputPassword1" class="form-label">Mot de passe</label>
                            <input type="password" class="form-control" id="exampleInputPassword1" name="motdepasse">
                                <% if(messmdp.length> 0) {%>
                                <div class="alert alert-danger mb-2" role="danger">
                                    <%= messmdp %>
                                </div>
                                <%}%>
                        </div>

                        <button type="submit" class="btn btn-primary mb-2">Connexion</button>
                    </form>

                    <button class="btn btn-primary">Retour sur la page principale</button>
                </div>

            </div>

        </div>
    </div>
    <%- include('partials-admin/footer') %>